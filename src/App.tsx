import { Button, Container } from "@mui/material";

function App() {
  return (
    <Container maxWidth="xl">
      <Button fullWidth variant="contained">
        Hola mundo
      </Button>
    </Container>
  );
}

export default App;
